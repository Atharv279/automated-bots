# Daily automated market signal generation
name: Daily Market Signals
on:
  schedule:
    - cron: "30 2 * * *"
    - cron: "30 8 * * *"
    - cron: "30 14 * * *"
    - cron: "30 20 * * *"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install -r dev-requirements.txt
      - name: Lint
        run: ruff check main.py --select=E,F --ignore=E501
      - name: Run bot
        run: python main.py
      - name: Commit and push
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
          git add logs/
          git diff --cached --quiet || git commit -m "automated-bots: market signals $(date -u +%Y-%m-%dT%H:%M)"
          git push || echo "Nothing to push"
